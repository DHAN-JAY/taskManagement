import React from 'react'

/**
 * Used to show the sidebar menu.
 @returns {*}
 @typedef Heading(string) value to be shown as a header in dialog 
 @typedef Private(boolean)
 @param {{
    
 }} props
*/

const Sidebar = () => {

    return (
        <div>
            
        </div>
    )
}

export default Sidebar